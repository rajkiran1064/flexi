name: flexi
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  bloc: ^9.0.0
  firebase_core: ^4.0.0
  firebase_messaging: ^16.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get: ^4.7.2
  http: ^1.5.0


dev_dependencies:
  lint: ^2.8.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
